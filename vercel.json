{
  "buildCommand": "npm run build -w packages/shared && npm run build -w web",
  "outputDirectory": "web/dist",
  "installCommand": "npm install",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
